@PACKAGE_INIT@

cmake_minimum_required(VERSION 3.2)

if(NOT TARGET circuitous)

  include(CMakeFindDependencyMacro)
  find_dependency(remill)

  # Exported Targets
  include("${CMAKE_CURRENT_LIST_DIR}/circuitousTargets.cmake")

endif()